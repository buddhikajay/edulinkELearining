{% extends "layout.html.twig" %}

{% block content %}
    {% trans_default_domain 'FOSUserBundle' %}

    <form action="{{ path('register_student') }}" method="POST">
        {{ form_widget(form) }}
        <div>
            <input type="submit" value="{{ 'registration.submit'|trans }}" />
        </div>
    </form>
{% endblock content %}